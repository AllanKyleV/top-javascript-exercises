// Counting Up
{
    let count = 1;
    while (count <= 10) {
        console.log(count);
        count++;
    }
}

// Even Numbers Only
{
    let count = 1;
    while (count <= 20) {
        if (count % 2 === 0) console.log(count);
        count++;
    }
}
